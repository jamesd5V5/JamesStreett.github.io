// index.js

document.addEventListener("DOMContentLoaded", function () {
    // Get the element with the ID "app" and set its innerHTML
    document.getElementById("app").innerHTML = "<h1>Hello, GitHub Pages with JavaScript!</h1>";
});
